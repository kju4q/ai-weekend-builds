# Past-You Searcher
#
# Verified together on Python 3.12.13 (macOS, arm64) on 2026-08-24.
# Both lines are optional in different ways, see the README:
#
#   sentence-transformers  real local embeddings. Without it the project falls
#                          back to a pure-Python hashing embedding and still
#                          runs, just with weaker matching. Installing it pulls
#                          in torch, which is a multi-GB download.
#   openai                 only used by the optional answer layer. Retrieval
#                          never touches it.

sentence-transformers==6.0.0
openai==3.3.1
