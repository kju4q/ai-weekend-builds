# STUB. Finalized during the build.
# Copy to .env and fill in. Every variable this project reads will be listed
# here, so no key is ever required without being documented.

# This project needs no model key. If the build introduces a variable
# (a port, a file path, a weather location), it gets documented here.
