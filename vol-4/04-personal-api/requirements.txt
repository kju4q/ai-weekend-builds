# Personal API
#
# No third-party dependencies. This project uses only the Python standard
# library: http.server, json, argparse, urllib, datetime, pathlib, threading,
# uuid, shutil.
#
# Running `python3 -m pip install -r requirements.txt` against this file
# downloads nothing and installs nothing. It is here so the documented setup
# sequence is the same as every other project in the volume.
#
# Verified on Python 3.12.13 (macOS 26.5.1, Apple M2 Pro) on 2026-08-24.
