# No third-party dependencies.
# This project uses only the Python standard library.
#
# simulate.py imports: argparse, datetime, html, json, math, pathlib, random,
# statistics, sys. The report is generated HTML, CSS, and inline SVG, so there is
# no plotting library to install.
#
# Verified on Python 3.14.3 and 3.12.13 (macOS 26.5.1, Apple M2 Pro) on 2026-08-24.
